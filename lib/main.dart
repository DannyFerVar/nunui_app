import 'package:flutter/material.dart';

void main() {
  runApp(const NunuiApp());
}

class NunuiApp extends StatelessWidget {
  const NunuiApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
